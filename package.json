{
  "name": "xlab-refresh-token",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm --prefix client run dev",
    "build": "npm --prefix client run build",
    "tauri:dev": "npm --prefix client run tauri:dev",
    "tauri:build": "npm --prefix client run tauri:build",
    "check": "npm --prefix client run build && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {}
}
